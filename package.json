{
  "name": "@chuangz/react-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "create-react-template": "bin/create-react-template.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "generate": "node bin/create-react-template.mjs",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "docs:links": "node scripts/check-links.mjs",
    "quality": "npm run lint && npm run format:check && npm run docs:links && npm test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.61.1",
    "eslint": "10.6.0",
    "globals": "17.7.0",
    "prettier": "3.9.1"
  },
  "engines": {
    "node": "^20.19.0 || >=22.13.0"
  }
}
